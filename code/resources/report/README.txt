Per vedere il report è necessario un browser con Javascript attivo. Bisogna servire il contenuto di questa cartella
con un server HTTP, per esempio con Python3

    python -m http.server

e poi aprire index.html nel browser (per esempio di default il server di Python è accessibile con http://localhost:8000
